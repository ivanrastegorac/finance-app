import { Heading, VStack, Flex, Avatar, Box } from "@chakra-ui/react";
import React from "react";

const Dashboard: React.FC = () => {
  return (
    <Box ml="250px" p={8}>
      <Heading mb={8}>Home Feed</Heading>
    </Box>
  );
};

export default Dashboard;
